---
kafka:
  protobuf_format: json-pretty
  bootstrap_servers:
    - localhost:9092

sources:
  - type: mqtt
    broker:
      host: mqtt.hsl.fi
      port: 8883
    processor: hsl-realtime
    config:
      vehicle_types: bus,tram
      event_types: arr,dep,vp
