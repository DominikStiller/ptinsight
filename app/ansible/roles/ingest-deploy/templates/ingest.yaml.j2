---
kafka:
  protobuf_format: json
  bootstrap_servers:
    {{ kafka_servers | to_nice_yaml }}

sources:
  - type: mqtt
    broker:
      host: mqtt.hsl.fi
      port: 8883
    processor: hsl-realtime
    config:
      vehicle_types: bus,tram
      event_types: arr,dep,vp
